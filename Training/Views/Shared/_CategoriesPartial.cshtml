@model IEnumerable<Training.Dto.CategoryDto>

<ul>
    @foreach (var category in Model)
    {
        <li>
            @Html.ActionLink(category.Name, "Index", "Posts", new { category = category.Name }, null)
        </li>
    }
</ul>
